import { AppText, Layout } from "components";
import React from "react";

export const ProfileScreen = () => {
  return (
    <Layout>
      <AppText>Cá nhân (Profile)</AppText>
    </Layout>
  );
};
